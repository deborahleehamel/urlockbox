<h1>Edit This Link</h1>
<div id="edit-form">
	<%= form_for (@link) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :url, "Url" %>
	<%= f.url_field :url %>
	<%= f.submit "Update Link", class: "btn btn-default" %>
	<% end %>
</div>
